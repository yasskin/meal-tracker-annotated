// the main.ts file loads our app component

import {bootstrap} from 'angular2/platform/browser'; // imports a function called bootstrap from Angular 2 library.

import {AppComponent} from './app.component'; // load our new component from its file path

bootstrap(AppComponent) // passes the AppComponent into the bootstrap function. 
